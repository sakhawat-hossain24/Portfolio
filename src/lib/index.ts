/**
 * Library index file - place files you want to import through the `$lib` alias in this folder.
 * This serves as the main entry point for the lib directory.
 * 
 * Common patterns:
 * - Export components from here for easy importing
 * - Export utility functions and helpers
 * - Export types and interfaces
 * - Re-export commonly used items from subdirectories
 */

// Example exports (uncomment and modify as needed):
// export { default as Button } from './components/Button.svelte';
// export { formatDate } from './utils/date';
// export type User from './types/user';
